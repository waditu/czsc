import pandas as pd
from typing import Any

def to_standard_kline_format(df: pd.DataFrame, dt_col: str = 'dt', open_col: str = 'open', high_col: str = 'high', low_col: str = 'low', close_col: str = 'close', volume_col: str = 'vol') -> pd.DataFrame: ...
def pivot_weight_data(dfw: pd.DataFrame, fill_value: float = 0.0) -> pd.DataFrame: ...
def resample_to_period(df: pd.DataFrame, period: str, agg_dict: dict[str, str] | None = None) -> pd.DataFrame: ...
def normalize_symbol(symbol: str) -> str: ...
def convert_dict_to_dataframe(data: list[dict[str, Any]], sort_by: str | None = None) -> pd.DataFrame: ...
def ensure_datetime_column(df: pd.DataFrame, column: str, inplace: bool = False) -> pd.DataFrame: ...
def flatten_multiindex_columns(df: pd.DataFrame, sep: str = '_') -> pd.DataFrame: ...
