# 任务计划

## 目标
- 读取飞书任务详情并确认开发目标
- 在 czsc 仓库完成对应开发
- 将关键进度同步给飞书任务负责人

## 阶段
- [x] 读取飞书任务与负责人信息
- [x] 明确需求并定位代码范围
- [x] 实现代码修改
- [x] 运行验证
- [ ] 汇报结果并收尾

## 当前任务摘要
- 任务标题：使用 rs-czsc 的模块替换掉 czsc 中现有的一些模块
- 任务 GUID：70d0f90e-183a-499f-80c5-f42d04a18ad4
- 关键要求：删除 czsc 中 signals 模块；对于 czsc.py 中已被 rs-czsc 实现覆盖的对象/模块，删除 Python 实现并改为使用 rs-czsc；同步更新相关单元测试并保证通过。

## 错误记录
| 错误 | 尝试 | 处理 |
|---|---:|---|
| `pip` 不可用 | 1 | 改用 `uv pip --python <venv>` 完成卸载与安装 |
| `+messages-send` 参数不兼容 | 1 | 改用 `--user-id` + `--text` 发送私信 |
